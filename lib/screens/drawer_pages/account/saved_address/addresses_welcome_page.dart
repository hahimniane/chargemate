import 'package:flutter/material.dart';

class AddressesHomePage extends StatelessWidget {
  const AddressesHomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
